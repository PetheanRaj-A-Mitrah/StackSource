'use strict';

/**
 * newsletter-sign-up router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::newsletter-sign-up.newsletter-sign-up');
